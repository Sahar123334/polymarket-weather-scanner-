Drop these 3 files on GitHub in one go.

1. Open your weather-bot repo on GitHub
2. Click Add file -> Upload files
3. Drag main.py, requirements.txt, and Procfile together (replace if asked)
4. Commit. Railway will redeploy.

Expect the next cron log to start with:
Weather Scanner + 3-rung Ladder + Settlement

Do not rename main.py. Leave PAPER_CSV=/data/paper_ledger.csv as-is.
